# SH

### podman_db.sh
> 使用Podman配置数据库(MySQL/PostgreSQL)

* via curl
```
sh -c "$(curl -fsSL https://raw.githubusercontent.com/EddyLee88/sh/main/scripts/podman_db.sh)"
```

* via wget
```
sh -c "$(wget https://raw.githubusercontent.com/EddyLee88/sh/main/scripts/podman_db.sh -O -)"
```
